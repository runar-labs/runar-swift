GOAL FIx PeerInfo CBOR decoding issue

THE appraoch is to test the struct CBOR both ways.. rust to swift an swift to rust to makle sure CBOR works properly and struct fields  names and types are proper also.. we use tehse tests for this 
/Users/rafael/dev/runar-swift/swift-ffi/Tests/SwiftFFITests/FFITypesCrossValidationTests.swift
and files under:
 /Users/rafael/dev/runar-swift/runar-rust/rust-examples
 CHECK EACH FILE CAREFULLY TO FIND whre this object shuold go.

 Context:  
**Sequence of Events:**
1. **Multicast message received** by Rust discovery system
2. **Rust processes the message** and logs "Processing announce message from peer"  
3. **Swift discovery polling loop** calls `pollDiscovered()`
4. **FFI call** `rn_discovery_poll_discovered()` returns data
5. **`copyBytesAndFree()`** creates a `Data` object from the FFI buffer
6. **`CodableCBORDecoder().decode(PeerInfo.self, from: data)`** is called
7. **CRASH** happens during `PeerInfo` decoding

**Exact Location of Crash:**
```swift
// In PeerInfo.init(from decoder:)
let publicKeyBytes = try container.decode([UInt8].self, forKey: .publicKey)
// ↑ CRASH HAPPENS HERE - Array index out of range
```

### **ROOT CAUSE: Data Format Mismatch**

The issue is a **data format mismatch** between what Rust sends and what Swift expects:

- **Rust side**: `public_key` field in CBOR format (unknown exact format)
- **Swift side**: Expects `public_key` as `[UInt8]` (array of bytes)
- **Problem**: `CodableCBORDecoder` tries to decode as array but the data format doesn't match
- **Result**: Array access fails with "Index out of range"

### **Evidence from Logs:**
```
[2025-10-02T10:41:21.202Z DEBUG] Processing announce message from peer
Swift/ContiguousArrayBuffer.swift:690: Fatal error: Index out of range
```

The crash happens **immediately after** "Processing announce message from peer", which means the issue is in the Swift CBOR decoding, not in the Rust processing.

More context:

I'll provide a comprehensive analysis of the Swift PeerInfo CBOR encoding/decoding compared to Rust, including all the details about the data that's causing the crash.

## **COMPREHENSIVE PEERINFO CBOR ANALYSIS**

### **1. Rust PeerInfo Structure**

Let me first check the Rust PeerInfo structure:


[1 tool called]

### **2. Swift PeerInfo Structure**

Let me check the Swift PeerInfo structure:


[1 tool called]

Let me get the complete Swift PeerInfo implementation:


[1 tool called]

### **3. Actual CBOR Data from Test**

From the test logs, I can see the exact CBOR data being generated by Rust and received by Swift:

**Rust CBOR Output (from logs):**
```
PeerInfo CBOR hex: [a2, 6a, 70, 75, 62, 6c, 69, 63, 5f, 6b, 65, 79, 98, 41, 04, 18, 99, 02, 18, c4, 18, 2b, 18, 1f, 18, 5c, 16, 18, a3, 18, 87, 0b, 18, 52, 18, 68, 18, b2, 18, 8f, 18, ae, 18, 66, 18, 94, 18, 39, 18, ce, 18, 70, 04, 18, c6, 18, ab, 18, 3d, 18, 9b, 18, 7f, 18, a3, 18, c1, 18, 30, 18, db, 18, 1a, 10, 18, 20, 15, 18, a1, 18, 41, 18, 1b, 18, 3e, 18, 33, 06, 18, d9, 08, 18, 68, 00, 00, 18, 47, 18, aa, 18, 1e, 18, 9e, 18, 5a, 18, 2c, 18, fe, 18, f4, 18, fc, 18, 1e, 18, ee, 18, b6, 18, 1e, 12, 18, 58, 18, d7, 18, ea, 18, cb, 18, ad, 69, 61, 64, 64, 72, 65, 73, 73, 65, 73, 81, 6f, 31, 32, 37, 2e, 30, 2e, 30, 2e, 31, 3a, 36, 33, 37, 32, 35]
```

**Swift CBOR Input (from logs):**
```
DEBUG: Received PeerInfo CBOR data length: 159
DEBUG: Received PeerInfo CBOR data hex: 
```

### **4. CBOR Structure Analysis**

Let me decode the CBOR structure:

**CBOR Structure:**
```
a2                    # Map with 2 key-value pairs
  6a 7075626c69635f6b6579  # Key: "public_key" (11 bytes)
  98 41                # Value: Array with 65 elements (0x41 = 65)
     04 1899 02 18c4 18 2b 18 1f 18 5c 16 18 a3 18 87 0b 18 52 18 68 18 b2 18 8f 18 ae 18 66 18 94 18 39 18 ce 18 70 04 18 c6 18 ab 18 3d 18 9b 18 7f 18 a3 18 c1 18 30 18 db 18 1a 10 18 20 15 18 a1 18 41 18 1b 18 3e 18 33 06 18 d9 08 18 68 00 00 18 47 18 aa 18 1e 18 9e 18 5a 18 2c 18 fe 18 f4 18 fc 18 1e 18 ee 18 b6 18 1e 12 18 58 18 d7 18 ea 18 cb 18 ad
  69 616464726573736573  # Key: "addresses" (9 bytes)
  81                    # Value: Array with 1 element
     6f 3132372e302e302e313a3633373235  # String: "127.0.0.1:63725"
```

### **5. Data Format Mismatch Analysis**

**The Problem:**
The Rust side is encoding the `public_key` as a **CBOR array of 65 individual bytes**, where each byte is encoded as a separate CBOR integer with the `18` prefix (which means "unsigned integer 0-23").

**The Swift side expects:**
- `[UInt8]` array (which should work)
- But the CBOR decoder is likely expecting a different format

### **6. Detailed CBOR Byte Analysis**

Let me analyze the specific bytes:

**Public Key Bytes (65 bytes):**
```
04 99 02 c4 2b 1f 5c 16 a3 87 0b 52 68 b2 8f ae 66 94 39 ce 70 04 c6 ab 3d 9b 7f a3 c1 30 db 1a 10 20 15 a1 41 1b 3e 33 06 d9 08 68 00 00 47 aa 1e 9e 5a 2c fe f4 fc 1e ee b6 1e 12 58 d7 ea cb ad
```

**Address:**
```
"127.0.0.1:63725"
```

### **7. The Root Cause**

The issue is in the **CBOR encoding format mismatch**:

1. **Rust encodes:** `Vec<u8>` as a CBOR array where each byte is encoded as a separate CBOR integer with `18` prefix
2. **Swift expects:** Either `[UInt8]` array or `Data` directly
3. **The crash happens:** When Swift's CBOR decoder tries to decode the array of individual CBOR integers into a `[UInt8]` array

### **8. Specific Crash Details**

**Crash Location:** `Swift/ContiguousArrayBuffer.swift:690: Fatal error: Index out of range`

**What's happening:**
- The CBOR decoder is trying to read 65 individual CBOR integers
- Each integer is prefixed with `18` (meaning "unsigned integer 0-23")
- The decoder is likely trying to access an array index that's out of bounds
- This suggests the decoder is not properly handling the array length or the individual integer decoding

### **9. Test Data Context**

**From the test logs:**
- **Node 1:** `45sou82038jmia9p07rkjvo3p4` at `127.0.0.1:63725`
- **Node 2:** `f2trpagkaealdqdh779ml30k0o` at `127.0.0.1:58046`
- **CBOR Length:** 159 bytes
- **Public Key Length:** 65 bytes (correct for ECDSA P-256)
- **Addresses:** 1 address per peer

### **10. Fix Requirements**

To fix this, you need to:

1. **Update Swift PeerInfo decoding** to handle the specific CBOR format that Rust generates
2. **Handle the `18` prefixed integers** properly in the CBOR decoder
3. **Ensure the array length calculation** is correct for the 65-byte public key
4. **Add proper bounds checking** in the CBOR decoding process

### **11. Recommended Fix Approach**

The Swift CBOR decoder needs to be updated to handle the specific format where:
- Each byte in the `public_key` array is encoded as a separate CBOR integer with `18` prefix
- The array length is correctly calculated (65 bytes)
- Proper bounds checking is implemented

This is a **CBOR format compatibility issue** between Rust's `serde_cbor` and Swift's `SwiftCBOR` library, not a data corruption issue.