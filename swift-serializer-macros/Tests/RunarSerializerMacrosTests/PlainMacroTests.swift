@testable import RunarSerializerMacros
import XCTest

final class PlainMacroTests: XCTestCase {
    func testBasicMacro() {
        // Basic test to ensure the macro package builds
        XCTAssertTrue(true)
    }
}
